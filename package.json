{
  "name": "shopsure",
  "version": "1.0.0",
  "description": "Lightweight Playwright TypeScript QA automation portfolio project.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "typescript": "^5.8.3"
  }
}
